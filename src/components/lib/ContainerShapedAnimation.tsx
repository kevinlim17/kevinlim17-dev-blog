import { keyframes } from '@emotion/react'

export const ContainerShapedAnimation = keyframes`
  0% { margin-left: -10vh; opacity: 0; }
  50% { margin-left: -5vh; opacity: 0; }
  100% { margin-left: 0vh; opacity: 1; }
`
